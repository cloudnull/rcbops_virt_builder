# Setup no roll back on failure
export DISABLE_ROLL_BACK=true

# Setup a master dev environment
export COOKBOOK_VERSION="master"

# Setup cidrs
export NOVA_INTERFACE="eth1"
export MANAGEMENT_INTERFACE="eth1"

# Setup passwords
export NOVA_PW="secrete"
export RMQ_PW="secrete"
export CHEF_PW="secrete"

# Set an override for my roles
export RUN_LIST="role[allinone],role[cinder-all],role[heat-all]"

# Disable all images
export CIRROS_IMAGE=False
export UBUNTU_IMAGE=False
export FEDORA_IMAGE=False
